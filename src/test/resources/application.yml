spring:
  datasource:
    membership:
      url: jdbc:mysql://localhost:3306/membership_db_test?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
      username: lvnvn
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        '[format_sql]': true
    show-sql: true
    open-in-view: false

  batch:
    jdbc:
      initialize-schema: always

  sql:
    init:
      mode: always
      platform: mysql
      schema-locations: classpath:config/mysql/init.sql

logging:
  level:
    '[com.burgerking]': INFO
    '[org.hibernate.SQL]': DEBUG
    '[org.hibernate.type.descriptor.sql.BasicBinder]': TRACE